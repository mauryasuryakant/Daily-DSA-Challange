// #include <bits/stdc++.h>
// using namespace std;int main(){vector<int>a={1,2,2,3,4},b={2,2,3,5},r;sort(a.begin(),a.end());sort(b.begin(),b.end());set_intersection(a.begin(),a.end(),b.begin(),b.end(),back_inserter(r));for(int x:r)cout<<x<<' ';}
